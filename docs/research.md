# mind_oribt_mvp 코드베이스 리서치 보고서

## 1. 결론 요약

이 저장소는 아직 “구현된 MVP”가 아니라, Vite + React + TypeScript 기반의 매우 얇은 프론트엔드 스캐폴드입니다. 현재 코드베이스를 깊이 읽은 결과, 제품 로직은 사실상 아직 시작되지 않았고, 런타임 구조도 최소 렌더링 부트스트랩 수준에 머물러 있습니다.

현재 확실하게 말할 수 있는 사실은 다음과 같습니다.

- 이 저장소는 프론트엔드 전용입니다.
- React 루트 컴포넌트는 하나뿐입니다.
- 실제 화면은 비어 있습니다. 이유는 루트 컴포넌트가 빈 Fragment만 반환하기 때문입니다.
- 백엔드, API 계층, 라우팅, 전역 상태관리, 영속 저장, 인증, 폼 처리, 테스트 스위트, 도메인 모델이 전혀 없습니다.
- `npm run lint`는 통과합니다.
- `npm run build`는 현재 실패합니다.
- 실패 원인은 `tsconfig.node.json`이 `vite.config.ts`를 포함 대상으로 기대하지만, 실제 파일이 존재하지 않기 때문입니다.
- 반면 `vite build` 자체는 성공하므로, 문제는 애플리케이션 번들링이 아니라 TypeScript 프로젝트 참조 설정에 있습니다.

한 줄로 정리하면 다음과 같습니다.

이 저장소는 MVP의 “시작점”은 맞지만, 아직 기능이 들어간 MVP는 아닙니다.

## 2. 저장소 인벤토리

현재 루트 기준 핵심 파일/폴더는 다음뿐입니다.

- `index.html`
- `package.json`
- `package-lock.json`
- `eslint.config.js`
- `tsconfig.json`
- `tsconfig.app.json`
- `tsconfig.node.json`
- `README.md`
- `docs/README.md`
- `src/main.tsx`
- `src/app/App.tsx`
- `src/app/App.css`
- `src/index.css`

중요하게 “없는 것”은 다음과 같습니다.

- `vite.config.ts` 없음
- `server`, `api`, `backend` 계층 없음
- 데이터베이스 스키마/마이그레이션 없음
- `.env` 파일 없음
- 테스트 파일 없음
- 정적 자산 폴더 없음
- `components`, `hooks`, `lib`, `services`, `store`, `context`, `types` 등의 구조 없음
- 페이지 단위 구성 없음

즉, 저장소 구조 자체가 매우 작고, 거의 모든 의미 있는 애플리케이션 계층이 아직 비어 있습니다.

## 3. 전체 아키텍처 구조

### 3.1 현재 아키텍처의 본질

현재 아키텍처는 “React SPA 부트스트랩” 그 자체입니다.

동작 순서는 다음과 같습니다.

1. `index.html`이 브라우저 문서 셸을 제공합니다.
2. 그 안의 `#root` DOM 노드가 React 마운트 지점이 됩니다.
3. `/src/main.tsx`가 브라우저에서 ES Module로 로드됩니다.
4. `src/main.tsx`가 React 루트를 만들고 `App`을 렌더링합니다.
5. `src/app/App.tsx`는 빈 Fragment를 반환합니다.
6. 결과적으로 화면에는 아무것도 출력되지 않습니다.

현재 구조를 한 줄로 표현하면 다음과 같습니다.

`Browser -> index.html -> Vite module entry -> React root -> empty App`

### 3.2 아키텍처 다이어그램

```text
브라우저
  -> index.html
     -> <div id="root"></div>
     -> <script type="module" src="/src/main.tsx">
        -> src/main.tsx
           -> src/index.css import
           -> src/app/App.tsx import
              -> src/app/App.css import
              -> 빈 Fragment 반환
           -> createRoot(...).render(
                <StrictMode>
                  <App />
                </StrictMode>
              )
```

### 3.3 아키텍처 관점에서 중요한 해석

이 저장소는 “레이어드 애플리케이션”이 아닙니다. 지금 존재하는 것은 제품 아키텍처가 아니라 렌더링 진입점뿐입니다.

현재 없는 계층은 다음과 같습니다.

- 도메인 계층
- 서비스 계층
- 데이터 접근 계층
- 상태 저장소
- 페이지 라우팅 계층
- 네트워크 계층
- 인증/세션 계층
- 비즈니스 규칙 계층
- 에러/로딩 처리 계층

즉, “전체 아키텍처 구조”를 분석하면 결론은 명확합니다.

현재 전체 구조는 단일 진입점 기반의 비어 있는 클라이언트 앱 골격입니다.

## 4. 각 주요 모듈의 역할

현재 코드 수가 매우 적기 때문에, 사실상 모든 파일을 주요 모듈로 간주해도 무방합니다.

### 4.1 `index.html`

역할:

- 브라우저가 처음 로드하는 정적 HTML 문서
- React가 붙을 `#root` 컨테이너 제공
- `src/main.tsx`를 모듈 진입점으로 연결

세부 관찰:

- `<html lang="en">`으로 설정되어 있음
- `<title>`은 `mind oribt`
- favicon `<link>` 태그는 있으나 `type=""`, `href=""`로 비어 있음
- 서버사이드 렌더링 흔적 없음
- 템플릿 엔진 없음

의미:

- 앱은 완전한 클라이언트 렌더링 방식입니다.
- 메타데이터 설정은 미완성 상태입니다.
- 프로젝트명/타이틀의 `oribt` 표기는 “orbit”의 오타일 가능성이 높아 보이지만, 브랜딩 의도일 수도 있으므로 확인이 필요합니다.

### 4.2 `src/main.tsx`

역할:

- 클라이언트 런타임의 실제 엔트리포인트
- React 트리 생성 및 마운트 담당

수행하는 일:

- `StrictMode` import
- `createRoot` import
- 전역 CSS(`src/index.css`) import
- 루트 컴포넌트(`src/app/App.tsx`) import
- `document.getElementById('root')!`에 React 앱 마운트

중요 디테일:

- `!` non-null assertion을 사용하므로 `#root`가 반드시 존재한다고 가정합니다.
- `StrictMode`가 켜져 있으므로, 향후 effect나 side effect가 들어오면 개발 모드에서 추가 검증 동작이 발생할 수 있습니다.
- 지금은 effect나 상태가 없어서 `StrictMode`의 눈에 띄는 영향은 없습니다.

### 4.3 `src/app/App.tsx`

역할:

- 전체 앱의 루트 컴포넌트

현재 구현:

- `./App.css` import
- 빈 Fragment 반환
- default export

의미:

- 이 컴포넌트는 구조상 모든 미래 기능이 시작될 자리입니다.
- 하지만 현재는 자식 컴포넌트도 없고, JSX 콘텐츠도 없고, 상태도 없고, props도 없고, 이벤트도 없습니다.
- 결과적으로 사용자 화면은 완전히 비어 있습니다.

### 4.4 `src/index.css`

역할:

- 전역 스타일 파일

현재 상태:

- 완전히 빈 파일

의미:

- CSS reset 없음
- 타이포그래피 시스템 없음
- 컬러 토큰 없음
- 레이아웃 기준 없음
- 글로벌 스타일 정책 없음

### 4.5 `src/app/App.css`

역할:

- `App` 컴포넌트 전용 스타일 파일로 의도된 파일

현재 상태:

- 완전히 빈 파일

의미:

- 루트 레이아웃 스타일 없음
- 앱 셸 시각 구조 없음
- 컴포넌트 수준 스타일 없음

### 4.6 `package.json`

역할:

- 프로젝트 스크립트와 의존성 정의

스크립트:

- `dev`: `vite`
- `build`: `tsc -b && vite build`
- `lint`: `eslint .`
- `preview`: `vite preview`

런타임 의존성:

- `react`
- `react-dom`

개발 의존성:

- `vite`
- `typescript`
- `@vitejs/plugin-react`
- `eslint`
- `@eslint/js`
- `typescript-eslint`
- `eslint-plugin-react-hooks`
- `eslint-plugin-react-refresh`
- `globals`
- `@types/react`
- `@types/react-dom`
- `@types/node`

중요 해석:

- 풀스택 모노레포가 아니라 단일 프론트엔드 앱 워크스페이스입니다.
- React 개발용 플러그인인 `@vitejs/plugin-react`가 선언되어 있지만, `vite.config.ts`가 없어서 실제로 설정에 연결되어 있지 않습니다.

### 4.7 `eslint.config.js`

역할:

- TypeScript/TSX 소스 코드 린트 규칙 정의

현재 설정 내용:

- `dist` 무시
- 적용 대상: `**/*.{ts,tsx}`
- JavaScript 추천 규칙 확장
- TypeScript ESLint 추천 규칙 확장
- React Hooks 추천 규칙 확장
- React Refresh 관련 규칙 확장
- 브라우저 글로벌 사용
- `ecmaVersion: 2020`

의미:

- 최소한의 프론트엔드 품질 가드는 준비되어 있습니다.
- 다만 적용 대상이 `ts`, `tsx`뿐이라 `eslint.config.js` 같은 JS 설정 파일 자체는 이 규칙 적용 대상이 아닙니다.

### 4.8 `tsconfig.json`

역할:

- 루트 TypeScript 솔루션 설정

내용:

- 직접 컴파일할 파일은 없음
- 다음 두 설정을 참조:
  - `tsconfig.app.json`
  - `tsconfig.node.json`

의미:

- 앱 코드와 노드 측 설정 파일을 분리한 프로젝트 참조 방식입니다.

### 4.9 `tsconfig.app.json`

역할:

- 브라우저용 앱 코드(`src`)에 대한 TypeScript 설정

핵심 옵션 해석:

- `target: es2023`
  - 최신 JS 문법 기준
- `lib: ["ES2023", "DOM", "DOM.Iterable"]`
  - 브라우저 환경 타입 포함
- `module: "esnext"`
  - ESM 우선 구성
- `types: ["vite/client"]`
  - Vite 클라이언트 타입 제공
- `moduleResolution: "bundler"`
  - Vite 같은 번들러 해석 방식에 맞춤
- `allowImportingTsExtensions: true`
  - `./app/App.tsx`처럼 확장자 포함 import 허용
- `verbatimModuleSyntax: true`
  - import/export 문법 보존 성향
- `moduleDetection: "force"`
  - 파일을 모듈로 강제 해석
- `noEmit: true`
  - TypeScript는 검사만 하고 출력물 생성은 하지 않음
- `jsx: "react-jsx"`
  - 최신 React JSX 변환 사용
- `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`
  - 비교적 엄격한 코드 위생 기준
- `erasableSyntaxOnly: true`
  - 런타임 변환이 필요한 TS 문법 사용을 제한

의미:

- 현재 앱 타입체킹 기반은 비교적 현대적이고 깔끔하게 잡혀 있습니다.
- 다만 실제 앱 코드가 거의 없어서 이 설정의 효용이 아직 드러나지 않습니다.

### 4.10 `tsconfig.node.json`

역할:

- 노드 측 설정 파일용 TypeScript 설정으로 의도된 파일

현재 핵심 문제:

- `include`가 `vite.config.ts`만 가리킵니다.
- 그런데 그 파일이 실제로 없습니다.

결과:

- `tsc -b`가 `TS18003: No inputs were found in config file`로 실패합니다.
- 그래서 `npm run build` 전체가 실패합니다.

의미:

- 이 저장소는 템플릿에서 생성된 뒤, `vite.config.ts`를 만들지 않았거나 삭제한 상태로 남아 있는 것으로 보입니다.
- 현재 저장소에서 가장 중요한 설정 결함입니다.

### 4.11 `README.md`, `docs/README.md`

역할:

- 루트 문서와 문서 폴더 진입점으로 의도된 파일

현재 상태:

- 둘 다 0바이트 빈 파일

의미:

- 설치 방법 문서 없음
- 제품 설명 없음
- 아키텍처 설명 없음
- 개발 규칙 문서 없음
- 기능 요구사항 문서 없음

## 5. 데이터 흐름 분석

### 5.1 실제 런타임 데이터 흐름

현재 데이터 흐름은 매우 단순합니다.

1. 브라우저가 `index.html` 로드
2. `#root` DOM 노드 확인
3. `/src/main.tsx` 실행
4. `main.tsx`가 CSS와 `App` import
5. React가 `<App />` 렌더
6. `App`이 빈 Fragment 반환
7. 렌더링 결과가 사실상 없음

즉, 현재는 “데이터가 흐르는 시스템”이라기보다는 “빈 루트가 마운트되는 시스템”입니다.

### 5.2 상태 흐름

현재 상태 흐름은 없습니다.

없는 요소:

- `useState`
- `useReducer`
- `useContext`
- 외부 스토어
- 서버 상태 캐시
- URL 상태
- localStorage/sessionStorage 사용

### 5.3 이벤트 흐름

현재 이벤트 흐름도 없습니다.

없는 요소:

- 버튼
- 입력 폼
- 클릭 핸들러
- 키보드 이벤트
- 마우스/포인터 상호작용
- effect 기반 구독 로직

### 5.4 네트워크 흐름

현재 네트워크 흐름도 없습니다.

없는 요소:

- `fetch`
- API 클라이언트
- Axios 등 HTTP 레이어
- WebSocket
- SSE
- RPC 호출
- 백엔드 엔드포인트

### 5.5 영속성 흐름

현재 영속성 흐름도 없습니다.

없는 요소:

- 데이터베이스
- 브라우저 저장
- 쿠키
- 세션
- 인증 토큰 저장

## 6. 의존성 관계 분석

### 6.1 런타임 의존성 관계

현재 실제 런타임에 의미 있게 작동하는 직접 의존성은 아래뿐입니다.

```text
애플리케이션 런타임
  -> react
  -> react-dom
```

관계 설명:

- `react`: 컴포넌트 모델 제공
- `react-dom/client`: `createRoot`를 통해 브라우저 DOM 마운트 제공

### 6.2 개발/빌드 의존성 관계

```text
개발 도구 체인
  -> vite
  -> typescript
  -> eslint
  -> @eslint/js
  -> typescript-eslint
  -> eslint-plugin-react-hooks
  -> eslint-plugin-react-refresh
  -> globals
  -> @types/react
  -> @types/react-dom
  -> @types/node
  -> @vitejs/plugin-react
```

각 역할:

- `vite`: 개발 서버, 번들링, 프리뷰 제공
- `typescript`: 타입 검사
- `eslint`: 정적 코드 품질 검사
- `typescript-eslint`: TS 규칙을 ESLint에 연결
- `eslint-plugin-react-hooks`: 훅 규칙 강제
- `eslint-plugin-react-refresh`: React Refresh 관련 규칙 지원
- `@types/*`: 타입 선언 제공
- `@vitejs/plugin-react`: React용 Vite 플러그인

중요 포인트:

- `@vitejs/plugin-react`는 설치돼 있지만 설정 파일이 없어 현재 명시적으로 연결되어 있지 않습니다.
- `@types/node`는 노드 측 설정 파일 타입용인데, 정작 그 핵심 대상인 `vite.config.ts`가 없습니다.

### 6.3 설정 간 의존성 관계

```text
package.json scripts
  -> dev     -> vite
  -> build   -> tsc -b -> tsconfig.json
                          -> tsconfig.app.json
                          -> tsconfig.node.json
             -> vite build
  -> lint    -> eslint . -> eslint.config.js
  -> preview -> vite preview
```

현재 실패 지점은 다음과 같습니다.

- `build`는 `tsc -b`에 의존함
- `tsc -b`는 `tsconfig.json`을 읽음
- `tsconfig.json`은 `tsconfig.node.json`을 참조함
- `tsconfig.node.json`은 없는 `vite.config.ts`를 기대함
- 따라서 전체 `build` 스크립트가 중단됨

### 6.4 현재 시점에서 사실상 미사용 또는 저활용 상태인 의존성

- `@vitejs/plugin-react`: 선언만 되어 있고 실제 설정 연결 없음
- React Hooks 관련 린트 규칙: 아직 훅이 없어서 실사용 없음
- `@types/node`: 노드 설정 파일 부재로 활용도 낮음

이것은 초기 스캐폴드에서는 흔하지만, 코드베이스가 아직 기능 구현 전 단계라는 증거이기도 합니다.

## 7. 중요한 비즈니스 로직

현재 중요한 비즈니스 로직은 존재하지 않습니다.

이 결론은 분석 부족이 아니라, 실제 코드 상태를 정확히 반영한 결과입니다.

현재 없는 로직은 다음과 같습니다.

- 사용자 여정 로직
- 인증/인가 로직
- 일정/추천 로직
- 저장/불러오기 로직
- 결제 로직
- 입력 검증 로직
- 도메인 규칙
- 데이터 변환 규칙
- 의사결정 엔진
- 예외 처리 정책

현재 존재하는 “유일한 로직”은 부트스트랩 로직뿐입니다.

- React 루트를 생성한다.
- `App`을 렌더링한다.
- `App`은 아무것도 렌더링하지 않는다.

즉, 비즈니스 로직 분석 섹션의 핵심 결과는 다음입니다.

이 저장소에는 아직 제품 도메인이 코드로 들어오지 않았다.

## 8. 실행 및 검증 결과

코드만 읽은 것이 아니라, 실제로 로컬에서 동작 확인도 수행했습니다.

### 8.1 의존성 설치 상태

초기 상태에서는 `node_modules`가 없었습니다.

그 결과:

- `npm run build`에서 `tsc`를 찾지 못함
- `npm run lint`에서 `eslint`를 찾지 못함

의존성 설치 후:

- 설치 성공
- 설치 로그 기준 취약점 없음

### 8.2 린트 결과

실행 명령:

```bash
npm run lint
```

결과:

- 성공

해석:

- 현재 존재하는 소스는 ESLint 규칙을 위반하지 않습니다.
- 다만 코드 양이 극도로 적기 때문에, 이것이 품질 성숙도를 뜻하지는 않습니다.

### 8.3 빌드 결과

실행 명령:

```bash
npm run build
```

결과:

- 실패

실패 원인:

- `tsconfig.node.json`의 `include: ["vite.config.ts"]`
- 실제 `vite.config.ts` 파일 부재
- `tsc -b`가 `TS18003`로 중단

해석:

- 앱 로직 자체가 깨진 것이 아니라, 빌드 체인의 설정이 어긋나 있습니다.

### 8.4 순수 Vite 번들 결과

실행 명령:

```bash
./node_modules/.bin/vite build
```

결과:

- 성공

해석:

- 현재 수준의 단순한 앱은 Vite 번들링 자체로는 문제없이 묶입니다.
- `npm run build`가 실패하는 진짜 원인은 TypeScript 프로젝트 참조 설정입니다.

### 8.5 개발 서버 결과

실행 명령:

```bash
npm run dev -- --host 127.0.0.1 --strictPort --port 4173
```

관찰 결과:

- Vite 개발 서버는 정상 기동

해석:

- 로컬 개발 시작 자체는 가능합니다.
- 다만 사용자 화면은 여전히 빈 페이지입니다.

## 9. 현재 사용자 관점의 실제 동작

지금 브라우저에서 이 앱을 열면 사용자는 다음 상태를 보게 됩니다.

- 브라우저 탭 제목은 `mind oribt`
- React 앱은 정상 마운트됨
- 화면에는 아무 UI도 없음
- 버튼, 텍스트, 네비게이션, 데이터, 인터랙션 전부 없음

즉, 현재 사용자 경험은 “열리긴 하지만 아무것도 없는 페이지”입니다.

## 10. MVP 관점에서 빠져 있는 핵심 요소

현재 코드베이스에 없는 중요한 구성은 다음과 같습니다.

- 기능 분해 구조
- 페이지 구조
- 라우팅 구조
- 재사용 컴포넌트 구조
- 상태관리 전략
- 데이터 모델
- API 계약
- 서버 연동 계층
- 인증/세션 설계
- 영속 저장 전략
- 에러/로딩 UI
- 분석/로그 체계
- 테스트 전략
- 제품 문서

즉, MVP를 만들기 위해 필요한 핵심 의사결정 대부분이 아직 코드에 반영되지 않았습니다.

## 11. 현재 상태가 의미하는 리스크와 제약

### 11.1 장점

- 코드베이스가 매우 작아서 방향 전환이 쉽습니다.
- 레거시 복잡도가 거의 없습니다.
- 구조를 처음부터 명확하게 설계하기 좋은 상태입니다.

### 11.2 단점

- 구현 진척도는 사실상 0에 가깝습니다.
- 빌드 스크립트부터 이미 설정 불일치가 있습니다.
- 문서가 없어 제품 의도가 저장소 안에 남아 있지 않습니다.
- 도구 체인은 깔려 있지만, 실제 기능 개발을 통해 검증된 적이 없습니다.

## 12. 실무적 해석

이 저장소를 가장 정확하게 설명하는 표현은 다음과 같습니다.

- “MVP 코드베이스”라기보다는 “MVP를 만들기 위한 프론트엔드 시작 템플릿”
- “제품 구현체”라기보다는 “런타임 진입점만 존재하는 스캐폴드”

즉, 지금 이 코드베이스를 완전히 이해한다는 것은 곧 다음 사실을 이해하는 것입니다.

현재 존재하는 것은 앱이 아니라 앱을 놓을 빈 틀이다.

## 13. 최종 평가

깊이 읽은 결과, 현재 저장소는 규모가 작기 때문에 오히려 완전하게 파악할 수 있습니다.

- 모든 소스 파일을 확인했습니다.
- 실행 진입 경로를 끝까지 추적했습니다.
- 의존성 관계를 확인했습니다.
- 빌드/린트/개발 서버 동작을 검증했습니다.
- 문서 부재와 비즈니스 로직 부재까지 포함해 현재 상태를 정리했습니다.

최종 진단은 다음과 같습니다.

이 저장소는 깔끔한 Vite/React/TypeScript 기반 프론트엔드 스캐폴드이지만, 아직 기능 구현이 없고, 루트 화면은 비어 있으며, `vite.config.ts` 부재로 인해 `npm run build`가 깨져 있는 초기 상태의 MVP 준비용 코드베이스입니다.

## 14. PRD 기반 제품 정의 해석

아래 내용은 사용자가 제공한 PRD를 바탕으로 정리한 제품 정의입니다. 이 섹션부터는 “현재 코드에 이미 구현된 것”이 아니라, “이 저장소가 앞으로 구현해야 하는 목표 상태”를 함께 다룹니다.

### 14.1 팀 핵심 원칙

PRD에서 가장 중요한 운영 원칙은 다음 두 문장으로 요약됩니다.

- 기능의 풍부함보다 단순함과 핵심 기능에 집중한다.
- 완성의 기준은 오직 모바일이다.

이 원칙은 단순한 슬로건이 아니라, 구현 우선순위와 QA 범위를 직접 제한하는 규칙입니다.

즉, 앞으로 이 코드베이스에 기능을 추가할 때 다음 기준이 강하게 적용되어야 합니다.

- 데스크톱 완성도보다 모바일 완성도가 우선
- 기능 추가보다 사용 마찰 제거가 우선
- 범용 협업 툴처럼 커지는 것보다, 한 가지 핵심 경험을 날카롭게 만드는 것이 우선

### 14.2 Product Core Message

PRD의 제품 핵심 메시지는 다음과 같습니다.

“생각정리 중관여자를 위한, 가장 가볍고 아름다운 마인드맵”

여기서 중요한 키워드는 다음 두 가지입니다.

- `Aesthetic Flow`
- `Zero Friction`

즉, 이 제품의 본질은 단순 기능 목록이 아니라 아래 두 경험 품질에 있습니다.

- 미적으로 숨 쉴 수 있는 화면
- 저장/조작/진입에서 사용자가 멈추지 않는 흐름

### 14.3 Product Vision

비전은 다음과 같습니다.

- 가장 가볍고 직관적인 생각의 구조화

즉, 무거운 협업 보드나 투박한 마인드맵 툴이 아니라, 빠른 사고 전개와 정리를 위한 경량 도구를 목표로 합니다.

### 14.4 제품 콘셉트

PRD가 정의한 콘셉트는 다음과 같습니다.

- Miro 같은 무거운 협업 툴에 지친 사용자
- SimpleMind 같은 투박한 마인드맵 경험에 아쉬움을 느끼는 사용자
- N형 창작자
- 이들을 위한 모바일 중심 반응형 마인드맵
- 현대적인 웹 UI/UX로 재해석
- 빠르고 안정적인 사용감 제공

즉, 제품 포지셔닝은 “협업 보드”가 아니라 “개인 사고 정리 도구”이며, “고기능성”보다 “가볍고 아름답게 생각을 펼치는 경험”이 중요합니다.

### 14.5 MVP Goal (Sprint 6)

PRD가 정의한 Sprint 6 MVP 목표는 아래와 같습니다.

- 서버 통신 없이 동작
- `IndexedDB`만으로 구동
- 오프라인 완결형 MVP
- 구현은 범용적으로 짜되
- QA 및 디버깅의 최우선은 모바일 화면의 심미성과 터치 조작감

이 목표는 기술적으로 다음 제약을 의미합니다.

- 서버 API는 이번 스프린트 범위가 아님
- 클라우드 저장도 이번 스프린트 범위가 아님
- 오프라인 복구 신뢰성이 핵심 비즈니스 요구사항임
- 저장 버튼 없는 자동 저장이 핵심 UX 정책임

## 15. PRD 기반 개발 프로세스 및 R&R

이 섹션은 현재 코드 실행 로직이 아니라, 팀 운영 및 책임 배분에 관한 제품 맥락입니다. 구현 우선순위와 산출물 해석에 중요하므로 문서에 포함합니다.

### 15.1 제출 기한

- 제출 기한: 4월 16일(목) 19:00까지
- 제출 위치: 노션 프로젝트 페이지 업로드

이것은 제품 요구사항이라기보다 프로젝트 관리 제약이지만, MVP 범위를 공격적으로 줄여야 하는 직접 이유가 됩니다.

### 15.2 데일리 싱크 방식

- 매일 아침/저녁 회의를 통해 진행 상황 공유
- 병목 발생 시 즉시 도움 요청
- 목표는 병목 제거

즉, PRD는 기술 설계뿐 아니라 실행 전략도 “독립 병렬 개발”에 맞춰져 있습니다.

### 15.3 팀 R&R

- 하상은 (Tech Lead / Claude)
  - 월요일 내 초기 폴더 구조 세팅
  - React Flow 기반 `CustomNode.jsx` 등 핵심 UI 부품 선제작
  - 가이드라인 배포
- 나호준 (PM & Data / Codex)
  - 대시보드/에디터 와이어프레임 설계
  - `IndexedDB` 기반 자동 저장 비즈니스 로직(Hook) 구현
- 박유빈 (Front Builder / Cursor)
  - 와이어프레임 기반으로 UI와 DB를 통합해 실제 페이지 구현
  - 수요일 내내 모바일 터치 디버깅 주도

이 R&R을 코드 관점에서 해석하면, 앞으로 코드베이스에는 적어도 다음 세 덩어리가 필요합니다.

- UI 컴포넌트 계층
- `IndexedDB` 자동 저장 훅/스토리지 계층
- 페이지 조립 및 모바일 디버깅 중심의 화면 계층

현재 저장소는 이 세 계층 모두 아직 비어 있습니다.

## 16. PRD 기반 스코프 정의

### 16.1 Out of Scope: Future (Sprint 7 이후)

이번 스프린트에서 의도적으로 제외된 미래 기능은 다음과 같습니다.

- 노드별 메모(노트)
- 다양한 테마 & 템플릿
- 자동 포커스
- 노드 검색
- 대시보드 내 썸네일
- 고급 단축키
- Undo/Redo 스택 고도화
- 이미지 업로드
- 인포그래픽 변환

### 16.2 Out of Scope: Drop (절대 안 만듦)

PRD가 명시적으로 배제한 기능은 다음과 같습니다.

- 실시간 협업 및 동시 편집
- 개별 노드 커스터마이징
- 폴더 구조 관리
- 외부 공유
- 음성 기반 생성
- AI 기반 마인드맵 생성

이 배제 목록은 굉장히 중요합니다. 구현 중 범위가 흔들릴 때, 아래 기준으로 되돌아가야 합니다.

- 단일 테마 고정
- 개인용 로컬 툴
- 공유/협업 없음
- AI 생성 없음

즉, 제품 정체성은 “개인 사고정리용 오프라인 마인드맵”입니다.

## 17. PRD 기반 MVP 핵심 요구사항 상세 해석

이 섹션은 PRD의 Must-Have 항목을 구현 관점으로 풀어쓴 것입니다.

### 17.1 Feature 1: 대시보드

직접 요구사항:

- 썸네일 없는 텍스트/카드 기반 심플한 목록 대시보드
- 새 마인드맵 생성
- 기존 마인드맵 삭제

이 요구사항이 뜻하는 바:

- 대시보드는 갤러리형이 아니라 리스트/카드형이어야 함
- 시각적 복잡도보다 탐색 단순성이 중요함
- 맵 관리의 최소 단위는 “생성, 조회, 삭제”

현재 코드베이스 상태:

- 대시보드 없음
- 목록 렌더링 없음
- 생성/삭제 로직 없음

### 17.2 Feature 2: 데이터 & 저장 체계

직접 요구사항:

- 저장소는 `IndexedDB`
- 로컬 기반 구조화된 데이터 저장
- 저장 버튼 없이 변경 시마다 자동 저장
- 브라우저 재진입 시 즉시 복구

이 요구사항이 뜻하는 바:

- 저장 행위는 UI 이벤트가 아니라 시스템 기본 동작이어야 함
- 영속화는 “명시적 저장”이 아니라 “배경 저장”
- 복구는 선택 기능이 아니라 기본 경험
- 데이터 손실 방지가 제품 신뢰성의 핵심

이 요구사항에서 도출되는 핵심 비즈니스 규칙:

- 변경 사항은 가능한 한 짧은 지연으로 지속 저장되어야 함
- 앱 재방문 시 마지막 상태가 자연스럽게 이어져야 함
- 오프라인 상태가 예외가 아니라 정상 동작 시나리오여야 함

현재 코드베이스 상태:

- `IndexedDB` 연동 없음
- 저장 훅 없음
- 자동 저장 없음
- 복구 로직 없음

### 17.3 Feature 3: 모바일 중심 UX 및 캔버스 조작

직접 요구사항:

- 모바일 최적화가 최우선
- 데스크톱은 부가적
- 모바일 환경 완벽 동작에 디버깅 리소스 집중
- 핀치 줌
- 터치 앤 드래그 패닝
- 실제 스마트폰 기기 디버깅
- 단축키 없는 모바일 환경을 고려한 플로팅 `+` 버튼(FAB)

이 요구사항이 뜻하는 바:

- 모바일은 “반응형 대응” 수준이 아니라 제품 완성 기준 그 자체
- 노드 추가는 키보드가 아니라 터치 기반 전용 UI로 가능해야 함
- 캔버스 제스처가 핵심 사용 경험임

현재 코드베이스 상태:

- 모바일 레이아웃 없음
- 터치 이벤트 없음
- 핀치 줌 없음
- 패닝 없음
- FAB 없음

### 17.4 Feature 4: PC 캔버스 조작

직접 요구사항:

- 노드 CRUD
- 노드 이동
- 자유 형식 레이아웃
- 드래그 앤 드롭 배치
- `Tab`: 자식 추가
- `Enter`: 형제 추가/완료
- `Backspace/Delete`: 삭제
- 단, PC 단축키의 완벽한 엣지 케이스보다 모바일 안정성이 우선

이 요구사항이 뜻하는 바:

- PC는 지원 대상이지만, 최적화 우선순위는 모바일보다 낮음
- 데스크톱에서 생산성 있는 편집은 가능해야 함
- 그러나 품질 기준의 1순위는 아니다

현재 코드베이스 상태:

- 노드 개념 없음
- 캔버스 없음
- 레이아웃 모델 없음
- 키보드 단축키 없음

## 18. PRD 기반 사용자 여정 분석

PRD가 정의한 사용자 여정은 아래와 같습니다.

1. 랜딩 페이지 진입
2. `시작하기` 클릭
3. 대시보드에서 기존 맵 선택 또는 `+ 새 마인드맵` 생성
4. 에디터에서 Root 노드를 중심으로 무한 캔버스 조작
5. 변경 시마다 `IndexedDB`에 자동 저장
6. 홈 버튼 클릭 시 최신 상태가 저장된 대시보드로 이동

이 플로우에서 직접 확정되는 화면 단위는 최소 3개입니다.

- 랜딩 페이지
- 대시보드 페이지
- 에디터 페이지

현재 코드베이스 상태:

- 랜딩 페이지 없음
- 대시보드 페이지 없음
- 에디터 페이지 없음
- 페이지 간 이동 구조 없음

## 19. PRD 기반 디자인 및 UI/UX 원칙

### 19.1 디자인 방향

직접 요구사항:

- Modern & Breathable
- 투박한 거미줄 디자인 배제
- 둥근 모서리
- 부드러운 그림자
- 여백 강조
- 현대적이고 심미적인 디자인

이 원칙은 기능보다 시각 경험을 규정합니다. 따라서 앞으로의 UI 구현은 단순히 작동만 하면 되는 것이 아니라, 다음 속성을 가져야 합니다.

- 정보량이 적절히 정돈된 레이아웃
- 시선이 막히지 않는 여백 구조
- 과도하게 복잡한 연결선/패널/도구 모음 억제

### 19.2 인지 부하 최소화 규칙

직접 요구사항:

- 상주 버튼 5개 이하 유지
- 텍스트(생각)가 돋보이도록 옅은 배경색 사용

이것은 단순한 디자인 취향이 아니라 제품 규칙입니다.

즉, 앞으로 구현 시 다음을 경계해야 합니다.

- 툴바 과밀화
- 항상 보이는 액션 버튼 남발
- 노드보다 UI가 더 튀는 화면

현재 코드베이스 상태:

- 아직 UI가 없으므로 이 원칙을 위반하지도, 충족하지도 않은 상태

## 20. PRD 기반 QA 성공 기준

PRD의 최종 완성 기준은 아래 3가지입니다.

### 20.1 Mobile First QA

체크 항목:

- 스마트폰 브라우저 접속 시 화면 깨짐 없음
- 터치 조작 원활
- 핀치 줌 원활
- 패닝 원활
- 플로팅 버튼으로 노드 추가가 마찰 없이 동작

제품적으로 의미하는 것:

- 모바일 시뮬레이션이 아니라 실제 스마트폰 브라우저 검증이 중요
- “작동한다”보다 “마찰 없이 자연스럽다”가 중요

### 20.2 Data Reliability QA

체크 항목:

- 인터넷을 끊은 오프라인 상태에서 노드 수정
- 새로고침 후에도 마지막 상태 완벽 복구

의미:

- 저장 신뢰성이 MVP의 성패를 좌우함
- 네트워크가 없어도 정상 제품 경험이어야 함

### 20.3 Submission Deadline QA

체크 항목:

- 4월 16일(목) 19시까지
- 구동 가능한 결과물
- 설명 포함
- 노션 프로젝트 페이지 정상 업로드

의미:

- 이번 스프린트에서 “완성”은 제품 철학과 모바일 안정성, 제출 가능성까지 포함한 실무적 정의입니다.

## 21. PRD로부터 직접 도출되는 핵심 비즈니스 로직

이 섹션은 PRD를 근거로 “반드시 구현되어야 하는 제품 규칙”을 추출한 것입니다.

### 21.1 저장 로직

- 저장 버튼이 없어야 한다.
- 변경은 자동으로 저장되어야 한다.
- 저장은 백그라운드에서 사용자 마찰 없이 이뤄져야 한다.
- 마지막 상태는 브라우저 재진입/새로고침 이후에도 복구되어야 한다.

### 21.2 마인드맵 관리 로직

- 사용자는 여러 개의 맵을 가질 수 있어야 한다.
- 대시보드에서 맵을 생성할 수 있어야 한다.
- 대시보드에서 맵을 삭제할 수 있어야 한다.
- 목록은 텍스트/카드 기반이어야 한다.

### 21.3 에디터 로직

- Root 노드를 중심으로 생각을 확장하는 구조여야 한다.
- 노드는 추가/편집/삭제/이동 가능해야 한다.
- 자유 형식 레이아웃이 가능해야 한다.
- 캔버스는 무한 공간처럼 작동해야 한다.

### 21.4 모바일 우선 로직

- 모바일이 최적화 1순위다.
- 모바일에서 노드 추가를 위한 전용 UI(FAB)가 있어야 한다.
- 터치 제스처가 핵심 인터랙션이다.

### 21.5 우선순위 로직

- 모바일 안정성이 PC 단축키 엣지 케이스보다 우선한다.
- 단순함이 기능 확장보다 우선한다.
- 심미성이 부가 요소가 아니라 핵심 가치다.

## 22. PRD 구현을 위해 필요한 목표 모듈 구조 추론

이 섹션은 PRD에 직접 파일명이 적혀 있지는 않지만, PRD를 충족하려면 합리적으로 필요하다고 추론되는 구조입니다. 아래 내용은 “직접 명시”가 아니라 “구현을 위한 설계 추론”입니다.

필요해 보이는 최소 화면 모듈:

- 랜딩 페이지
- 대시보드 페이지
- 에디터 페이지

필요해 보이는 최소 기능 모듈:

- 맵 목록 조회/생성/삭제 로직
- `IndexedDB` 저장소 래퍼
- 자동 저장 훅
- 앱 초기 복구 로직
- 노드/엣지 상태 모델
- 캔버스 뷰포트 상태 모델
- 모바일 FAB 컴포넌트
- 홈 버튼/페이지 이동 로직

필요해 보이는 최소 데이터 구조 추론:

- 마인드맵 엔티티
  - `id`
  - `title`
  - `createdAt`
  - `updatedAt`
- 노드 엔티티
  - `id`
  - `mapId`
  - `parentId`
  - `text`
  - `position`
- 에디터 뷰 상태
  - `zoom`
  - `pan`
  - 선택된 노드 등

위 필드들은 PRD에 직접 명시되지는 않았지만, PRD 요구사항을 만족하려면 거의 필수에 가깝습니다.

## 23. 현재 코드베이스와 PRD의 정합성 평가

이제 현재 코드베이스를 PRD와 직접 비교하면 다음과 같습니다.

### 23.1 이미 부합하는 부분

- React 기반 클라이언트 앱 시작점은 있음
- 로컬 프론트엔드 중심으로 확장하기 쉬운 작은 구조임
- 서버 코드가 없으므로 오프라인 중심 MVP로 방향 전환하기 쉬움

### 23.2 아직 전혀 구현되지 않은 부분

- 랜딩 페이지
- 대시보드
- 에디터
- 노드 모델
- 캔버스 인터랙션
- `IndexedDB`
- 자동 저장
- 재복구 로직
- 모바일 FAB
- PC 단축키
- 디자인 시스템/시각 언어

### 23.3 PRD와 충돌하거나 즉시 보완이 필요한 부분

- `npm run build`가 현재 실패하므로 제출 가능한 상태가 아님
- `vite.config.ts`가 없어서 설정 일관성이 깨져 있음
- 문서 파일이 비어 있어 팀 가이드라인 공유 기반이 약함
- 현재 UI가 완전히 비어 있어 PRD의 “Aesthetic Flow”를 검증할 대상이 없음

## 24. PRD 반영 후 최종 진단

PRD를 포함해 이 저장소를 다시 해석하면, 현재 프로젝트는 단순한 빈 템플릿이 아니라 다음 목표를 향한 “미구현 골격”입니다.

- 모바일 우선 오프라인 마인드맵
- `IndexedDB` 기반 자동 저장
- 랜딩 -> 대시보드 -> 에디터 흐름
- 심미적이고 가벼운 사용감
- 최소 기능 집중 전략

하지만 현재 코드베이스는 아직 그 목표를 실제로 구현하기 전 단계입니다.

따라서 최종적으로 가장 정확한 판단은 다음과 같습니다.

이 저장소는 PRD상 매우 명확한 방향성을 가진 모바일 우선 오프라인 마인드맵 MVP의 출발점이지만, 현재 구현 수준은 React/Vite 부트스트랩에 머물러 있으며, PRD의 핵심 가치인 `Aesthetic Flow`, `Zero Friction`, `IndexedDB` 자동 저장, 대시보드/에디터 사용자 여정, 모바일 터치 UX는 아직 코드로 실현되지 않은 상태입니다.
